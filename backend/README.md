# Backend (Spring Boot)

This folder will contain the Java 21+ Spring Boot implementation.

Activation note:
- Quality gates (acceptance/e2e/arch testing) become *enforceable* when real backend code is introduced (see ADR-0002).
