# API Specs (Derived)

APIs are projections of the domain, not drivers.

Notes:
- Authoritative intent lives in domain + requirements specs.
- OpenAPI/AsyncAPI/SDKs are generated into `docs/derived/`.
- If you must describe API shape here, keep it non-authoritative and generator-oriented.
