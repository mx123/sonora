# Messaging Specs (Contracts)

Messaging technology is deployment-dependent; this folder stores *contracts* and message schemas used by adapters.

Rules:
- No vendor-specific runtime assumptions in core.
- Prefer stable message contracts + versioning rules.
